# 环境变量示例文件
# 此文件仅作为参考，实际配置请使用：
# - 后端配置：.dev.vars.example → .dev.vars
# - 前端配置：.env.local.example → .env.local

# ===== Cloudflare Workers 环境变量 (.dev.vars) =====
EMAIL_DOMAIN=your-domain.com
DATABASE_ID=your-database-id

# ===== 前端环境变量 (.env.local) =====
# API 接口地址（本地开发）
VITE_API_URL=http://localhost:8787
# 项目信息
VITE_APP_NAME=Email4Cursor
VITE_GITHUB_URL=https://github.com/yourusername/Email4Cursor
# 访问密码（留空则不启用密码验证）
VITE_ACCESS_CODE= 